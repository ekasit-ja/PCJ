<div class="container">
    <h2 class="header">manage product type</h2>

    <a href="<%= sails.getUrlFor("ProductController.typeCreate") %>" class="btn btn-default">
        Create
    </a>
    <a href="<%= sails.getUrlFor("ProductController.manager") %>" class="btn btn-primary">
        Back
    </a>
    <hr>

    <table class="table table-striped admin-table">
        <thead>
            <tr>
                <th>Category</th>
                <th>Title</th>
                <th>Image</th>
                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            <% _.each(types, function(type) { %>
                <tr>
                    <td><%= sails.config.enums.category[type.category] %></td>
                    <td><%= type.title %></td>
                    <td><img src="<%= type.image %>"></td>
                    <td>
                        <a href="<%= sails.getUrlFor("ProductController.typeUpdate").replace(":tid", type.id) %>" class="btn btn-primary"><i class="fa fa-pencil" aria-hidden="true"></i></a>
                    </td>
                    <td>
                        <form method="post" action="<%= sails.getUrlFor('ProductController.typeDelete') %>">
                            <input type="hidden" name="tid" value="<%= type.id %>">
                            <button type="submit" class="btn btn-danger"><i class="fa fa-trash-o" aria-hidden="true"></i></button>
                        </form>
                    </td>
                </tr>
            <% }) %>
        </tbody>
    </table>
</div>
