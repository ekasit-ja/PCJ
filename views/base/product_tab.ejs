<ul class="nav nav-tabs">
    <li class="active"><a data-toggle="tab" href="#desc">description</a></li>
    <li><a data-toggle="tab" href="#cert">certificate</a></li>
    <li><a data-toggle="tab" href="#inst">installation</a></li>
    <li><a data-toggle="tab" href="#catg">catalogue</a></li>
</ul>

<div class="tab-content">
    <div id="desc" class="tab-pane fade in active">
        desccription
    </div>
    <div id="cert" class="tab-pane fade">
        <%if(certs.length>0) {%>
            <%_.each(certs, function(cert) {%>
                <div class="btn-cert">
                    <a href="<%=cert.url%>" target="_blank">
                        <div class="img-frame"><img src="<%=cert.image%>"></div>
                        <div class="title">download</div>
                    </a>
                </div>
            <%})%>
        <%} else {%>
            There is no file available at this time.
        <%}%>
    </div>
    <div id="inst" class="tab-pane fade">
        <%if(insts.length>0) {%>
            <%_.each(insts, function(inst) {%>
                <div class="btn-cert">
                    <a href="<%=inst.url%>" target="_blank">
                        <div class="img-frame"><img src="<%=inst.image%>"></div>
                        <div class="title">download</div>
                    </a>
                </div>
            <%})%>
        <%} else {%>
            There is no file available at this time.
        <%}%>
    </div>
    <div id="catg" class="tab-pane fade">
        <%if(catgs.length>0) {%>
            <%_.each(catgs, function(catg) {%>
                <div class="btn-cert">
                    <a href="<%=catg.url%>" target="_blank">
                        <div class="img-frame"><img src="<%=catg.image%>"></div>
                        <div class="title">download</div>
                    </a>
                </div>
            <%})%>
        <%} else {%>
            There is no file available at this time.
        <%}%>
    </div>
</div>
<hr>
